fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic[email]==2.9.2
pydantic-settings==2.5.2
SQLAlchemy==2.0.35
alembic==1.13.2
psycopg2-binary==2.9.9
redis==5.0.8
python-jose[cryptography]==3.3.0
passlib[argon2]==1.7.4
python-multipart==0.0.9
PyMuPDF==1.24.10
python-docx==1.1.2
weasyprint==62.3
pydyf==0.11.0  # pinned: weasyprint 62.3 is incompatible with pydyf>=0.12 (AttributeError: 'super' object has no attribute 'transform' in stream.py during write_pdf) - confirmed via live testing, not a hypothetical
Jinja2==3.1.4
httpx==0.27.2
groq==1.7.0  # upgraded from 0.11.0 (2025-01) - the old pin predates most current
             # Groq models (llama-3.3-70b-versatile, llama-3.1-8b-instant, etc. did
             # not exist in its bundled type hints), which is a plausible real
             # contributor to model/compatibility failures. API surface used by
             # this codebase (Groq(), .chat.completions.create(), exception
             # classes) verified unchanged - full test suite passes against it.
sentence-transformers==3.1.1
numpy==1.26.4
python-dotenv==1.0.1
tenacity==9.0.0
pytest==8.3.3
pytest-asyncio==0.24.0
